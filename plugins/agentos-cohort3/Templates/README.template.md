# <Agent Name>

This folder contains the scoped AgentOS agent for <purpose>.

Create this README only when the folder needs human onboarding, several subfolders, or handoff guidance beyond `PROJECT.md` and `AGENTS.md`.

## Core Files

- `PROJECT.md` defines ownership, boundaries, source systems, risks, and success.
- `AGENTS.md` defines scoped operating behavior.

## Optional Folders

- `skills/` contains repeated workflows.
- `runbooks/` contains operating procedures, setup, demos, or recovery steps.
- `memory/` stores curated durable facts when needed.
- `logs/` stores dated operating or verification notes when needed.
- `templates/` stores repeated artifact templates when needed.
- `audits/` stores structured reviews when needed.

If an optional folder is not present, it has not earned a current use yet.

## First Use

1. Read `PROJECT.md`.
2. Read `AGENTS.md`.
3. Read the relevant local skill or runbook only if the task requires it.
